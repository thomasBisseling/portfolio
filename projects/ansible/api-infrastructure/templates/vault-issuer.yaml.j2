apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: vault-issuer
spec:
  vault:
    server: "vault.{{ host }}"
    path: "pki/sign/kubernetes"
    auth:
      kubernetes:
        role: cert-manager
        mountPath: /v1/auth/kubernetes
